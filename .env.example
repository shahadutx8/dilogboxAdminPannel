# PostgreSQL connection string
  DATABASE_URL=postgresql://user:password@localhost:5432/dialog_panel

  # JWT secret — যেকোনো random string দিন (গোপন রাখুন)
  SESSION_SECRET=your-very-secret-key-change-this

  # Server port (default: 3000)
  PORT=3000
  